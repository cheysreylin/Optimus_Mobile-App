import 'package:flutter/material.dart';

class MainPartnership extends StatelessWidget {
  const MainPartnership({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}